module IngestionFormatters
# Handle tower configuration geometries, surface roughness maps, and file profiles
export load_campaign_metadata

function load_campaign_metadata(campaign::String)
    # Placeholder for geometry setups (z0m, d, height channels)
    println("Configuring profiles for campaign: ", campaign)
end

end # module
