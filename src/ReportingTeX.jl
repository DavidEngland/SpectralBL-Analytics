module ReportingTeX
# Layout compilers to target downstream manuscript files
export export_to_tex_table

function export_to_tex_table(output_path::String, data)
    # Outputs formatted LaTeX matrix macros or structural tables
end

end # module
